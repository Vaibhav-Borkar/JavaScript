var ab = 12;
var ba = 24;
console.log(ab + ba);

// + - % * /
// += -= etc.
// && || !

let a = 12;
let b = "12";

// check two values are eqaul  after performing type conversion if nessesary
console.log(a == b); // true

// check two values are equal without performing type conversion
console.log(a === b); // false

// check two values are not  equal after performing type conversion if nessesary
console.log(a != b); // false

// check two values are not equal , without performing type conversion
console.log(a !== b); // true
