let std_marks = [78, 98, 98, 89, 67, 75, 78, 95, 65];
console.log(std_marks);

let mix = [23, 56, "java", null, undefined, Number, 45.6];
console.log(mix);

console.log(mix.length);

// we can change the values present inside array --->arrays are mutable

mix[0] = "vaibahv";
console.log(mix);
console.log(typeof mix);

// arrays are Object type in js
